# Workflow Rules

Conventions for how to work, regardless of what is being produced. Files in this directory describe collaboration, process, and discipline, distinct from artefact-side rules (`code/` for code, `writing/` for text).
